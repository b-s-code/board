import {describe, expect, test} from '@jest/globals';
import {BoardState} from './script';

describe('my test module', () => {
  test('not a very sophisticated test', () => {
    expect("hello").toBe("hello");
  });
});
